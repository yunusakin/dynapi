spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: dynapi-test
  
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  messages:
    basename: messages
    encoding: UTF-8

server:
  port: 0 # Random port for testing
